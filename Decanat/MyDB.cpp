#include "MyDB.h"
//#include "windows.h"

/*
void MyDB1::RawSQL(char *S) {
//	if (mysql_query(&mysql, S)) return;
//	if (!(res = mysql_store_result(&mysql))) return;
	
}
void MyDB1::Connect() {
	mysql_init(&mysql);

	if (!(mysql_real_connect(&mysql, "localhost", "root", "root", "test", 3306, NULL, 0)))return;
	if (mysql_select_db(&mysql, "test")) return;	
		printf("4 ");
		while ((row = mysql_fetch_row(res))) {
			printf("5 ");
			for (i = 0; i < mysql_num_fields(res); i++)
				printf("%s\n", row[i]);
		}
		if (!mysql_eof(res)) exiterr(5);

	
}



void MyDB1::Disconnect() {
//	mysql_free_result(res);
//	mysql_close(&mysql); 
}
*/

